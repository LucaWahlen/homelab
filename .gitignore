# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log

# Ansible
*.retry
ansible/inventory/hosts.yml
ansible/inventory/group_vars/all.yml
kubeconfig
*.kubeconfig

# Kustomize caches pulled Helm charts (tgz + unpacked) next to the
# kustomization when helmCharts has a repo: field. Regenerated on every build.
apps/argocd/manifests/charts/

# Secrets / keys
*.pem
*.key
.vault_pass

# OS / editor
.DS_Store
.idea/
.vscode/
