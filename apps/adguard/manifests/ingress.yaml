apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: adguard
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`adguard.lucawahlen.com`)
      kind: Rule
      services:
        - name: adguard
          port: 80